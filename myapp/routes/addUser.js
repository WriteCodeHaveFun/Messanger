var express = require('express');
var router = express.Router();


router.post('/', function(req, res) {
    console.log('addUser functionality is not ready yet');
    // res.send('addUser functionality is not ready yet')
    // res.render()
});

module.exports = router;